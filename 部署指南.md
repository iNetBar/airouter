# iRouter 部署指南（小白版）

> **目标**：把 iRouter（Cloudflare Pages + D1）部署上线，**改代码自动部署，数据库自动建好**。
> **前提**：会复制粘贴命令即可，不需要懂 Cloudflare 原理。

---

## 一、一次性准备工作（只需做一次）

### 1. 准备账号
| 需要 | 怎么弄 | 多少钱 |
|---|---|---|
| GitHub 账号 | github.com 注册 | 免费 |
| Cloudflare 账号 | dash.cloudflare.com 注册 | 免费 |
| 一张卡（**只验证身份，不扣费**） | Cloudflare → 个人资料 → 付款方式 | Workers 免费档不需要，但 D1 创建**部分地区要求绑卡验证** |

### 2. 拿到两个密钥（填到 GitHub）
进入 https://dash.cloudflare.com/profile/api-tokens → **Create Token** → 选模板 **"Edit Cloudflare Workers"** → **确认后额外勾选 `D1:Edit`**（默认模板只含 Workers:Edit）→ 创建。**（部署到 Pages 需要勾选 `Account → Cloudflare Pages → Edit`，如提示权限不足可改选模板 "Edit Cloudflare Workers" 后手动添加 Pages:Edit）**。

你会拿到一串 `cf_...` 开头的 Token。

然后在仓库页面：**Settings → Secrets and variables → Actions → New repository secret**，添加两个：

| Name | Secret |
|---|---|
| `CF_API_TOKEN` | 刚才那串 `cf_...` |
| `CF_ACCOUNT_ID` | Cloudflare 控制台右下角 "Account ID" |

### 3. 填写数据库 ID（本地一次性）
```bash
npm install -g wrangler
wrangler login          # 浏览器跳转让你授权
wrangler d1 create irouter-db
```
复制输出的 `database_id`（32 位十六进制，形如 `a1b2c3...`），打开 `wrangler.toml`：
```toml
database_id = "a1b2c3..."   # 把 <DATABASE_ID> 换成真实值
```
**提交这个改动**。以后 GitHub Actions 自动建表、部署，都不用再碰本地。

### 4. 设置登录密码（部署后再做也行）
Cloudflare 控制台 → **Workers & Pages → irouter → Settings → Variables and Secrets** → 添加：
- `SESSION_SECRET`：随便一段长随机字符串（如 `openssl rand -hex 32` 生成）
- `DEFAULT_ADMIN_PASS`：**你登录后台的密码**（必改，否则是默认弱口令）
- `API_TOKEN`：可选，调用网关时用的 Token
- `ENCRYPT_KEY`：可选，加密供应商 Key 用（不设则用 SESSION_SECRET 派生）
- 也可以直接用 GitHub Actions 的 Secrets（`DEFAULT_ADMIN_PASS` 等），workflow 部署时会自动 `pages secret put`

> ⚠️ 这些是 **Secrets**，不会进代码、不会被 Actions 日志打印。

---

## 二、日常部署（全自动）

```bash
git add .
git commit -m "update"
git push
```
打开 GitHub → **Actions** 标签 → 看到绿色 ✓ → 部署完成。

Actions 会自动做：
1. 安装依赖
2. 打包 worker（`dist/_worker.js`）
3. **创建 D1 数据库**（若不存在）
4. **建表**（`schema.sql`，可反复跑，安全）
5. 创建 Pages 项目（若不存在）并部署

访问 `https://irouter.pages.dev/admin` 即可（也可在 Cloudflare → Workers & Pages → irouter → Deployment 查看每次部署的预览地址）。

---

## 三、常见错误速查

### ❌ `wrangler: command not found` (exit code 127)
**原因**：workflow 里直接调 `wrangler`，但它没装。
**本版本已修复**：全部改用官方 `cloudflare/wrangler-action@v3`，自带 wrangler，不会 127。
**若你自定义了 workflow**：确保在 `run: wrangler ...` 之前有：
```yaml
- run: npm install -g wrangler
```

### ❌ `Node.js 20 is deprecated`
无害警告，不影响部署。本版本已锁定 `node-version: '24'` + 设置 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`。

### ❌ `D1 binding not found / no such table`
两种可能：
1. `wrangler.toml` 的 `database_name` 与 Actions 里 `d1 create` 的名字**不一致**（本版本统一为 `irouter-db`）
2. 建表 step 没跑成功。**手动补一次**：
   ```bash
   wrangler d1 execute irouter-db --remote --file=./schema.sql
   ```

### ❌ `database_id` 占位符报错
`wrangler.toml` 里还是 `<DATABASE_ID>` 没替换。本地跑 `wrangler d1 create irouter-db` 拿 ID 填进去提交。

### ❌ 部署成功但 `/admin` 登录不了
没设 `DEFAULT_ADMIN_PASS`。到 Cloudflare 控制台 → Workers & Pages → irouter → **Settings → Variables and Secrets** 添加后**重新部署**（Actions 重跑一次，或改一行代码 push）。

---

## 四、免费额度参考（Cloudflare Free）

| 资源 | 额度 | 省额度技巧 |
|---|---|---|
| Pages Function 请求 | 10 万次/天 | 管理后台低频，够用 |
| CPU 时间 | 10ms/请求（每日总额） | **流式透传 LLM**，不 buffer 完整响应 → 单次 <5ms |
| D1 读 | **500 万行/天** | 进程级内存缓存 60s，`/admin/api/*` 日常几乎不读 D1 |
| D1 写 | **10 万行/天** | 日志攒批 60s 一次 flush |
| D1 存储 | **5 GB** | 日志表裁剪到 2000 条 |
| KV | 未使用（本版本纯 D1） | — |

详见 `COST.md`。

---

## 五、本地调试

```bash
npm install
cp wrangler.toml wrangler.toml.bak  # 可选备份
wrangler dev --local                # 本地起服务 + 本地 D1，改代码热更新
```
浏览器打开 `http://localhost:8787/admin`。

---

**一句话总结**：填 2 个 Secret → 本地跑一次 `wrangler d1 create` 拿 ID 填进 `wrangler.toml` → 以后 `git push` 全自动。
