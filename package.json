{
  "name": "irouter-cf-d1",
  "version": "3.7.0",
  "description": "iRouter — Cloudflare Workers + D1 (替代 Deno KV 版，小白一键部署)",
  "type": "module",
  "main": "worker.ts",
  "scripts": {
    "build": "node build.cjs",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler pages deploy dist --project-name irouter --branch main --commit-dirty=true",
    "migrate": "wrangler d1 execute irouter-db --remote --file=./schema.sql",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name irouter --branch main --commit-dirty=true"
  },
  "dependencies": {
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.131.1",
    "esbuild": "^0.24.0"
  }
}
