# 依赖
node_modules/

# 本地构建产物 (CI 会重新构建)
dist/
*.bundle.js

# wrangler 本地调试缓存（pages dev 生成的临时 bundle 与本地 D1 sqlite）
.wrangler/

# 环境变量 / 密钥 (绝不提交)
.env
.env.*
wrangler.toml.local
.dev.vars

# 编辑器 / 系统
.DS_Store
*.log
.vscode/
.idea/

# 浏览器自动化临时产物
.dumate/
